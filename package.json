{
  "name": "ouija-board-p2p",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run server:dev\" \"npm run dev -w client\"",
    "server:dev": "cd server && go run .",
    "server:build": "cd server && go build -o bin/server .",
    "build": "npm run server:build && npm run build -w client",
    "typecheck": "cd server && go vet ./... && cd .. && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
